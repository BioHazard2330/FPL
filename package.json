{
  "name": "fpl-agent-dashboard-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only tooling for visual QA of the static dashboard HTML. Not part of the Python runtime.",
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
