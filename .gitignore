.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# runtime data — never commit
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/raw/
data/cache/
data/tmp/
data/backups/
data/*.npz
data/*.joblib
data/dashboard.html
data/live_snapshot.json
data/*.lock
# real, regenerable via `fpl sync-crests` - never worth committing 20
# binary PNGs that a fresh checkout can re-fetch in seconds
data/crests/
logs/*.log
logs/*.log.*
*.cache/
.tmp/

# secrets
.env
*.env.local

# OS
.DS_Store
Thumbs.db

# Dev-only Playwright visual-QA tooling (2026-09-03, Phase 7)
node_modules/
package-lock.json

# Scratch / profiling artefacts (2026-09-09). These have been dodged by hand
# on every commit for several sessions - a raw scraped page kept for a
# one-off investigation, and timing dumps from cache-performance work.
# Neither is source, neither should ever land in the repo.
data/ffp_raw.html
perf_*.txt
