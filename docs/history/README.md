- [41-session-2026-09-02-football-intelligence-phase3-finalization.md](41-session-2026-09-02-football-intelligence-phase3-finalization.md) — 2026-09-02 (finish the 3 deferred detectors, formalize signal expiry, full suite, production verification, then stop): new `models/role_signal_detectors.py` — ROLE_CHANGE (real shots/xG profile-shift vs a player's own baseline, the honest substitute for `position`/`touches_box` confirmed 0/654 populated), SET_PIECE_CHANGE (real `player_setpiece_history` version-order changes, anchored to the one real match the change took effect in), TACTICAL_CHANGE (real formation departure from a team's own baseline, linked to player-level role signals in the same match) — all reusing `statistical_evidence.py`'s own cross-source dedup pattern, never a second pathway. Real bug found + fixed via production verification (not unit tests): the setpiece detector was re-firing the same change on every later match (76 spurious rows against real data, cut to 33 real ones after anchoring the fix). `FootballSignal` gained `last_confirmed_at` + category-aware `expires_at`/EXPIRED status (real match-count clock, 5 matches for role/setpiece, 3 for tactical), layered on top of the existing persistence classifier, never replacing it. Real, disclosed finding: correctly-anchored SET_PIECE_CHANGE signals are structurally capped at NEW_SIGNAL/MONITOR under the existing classifier (need a second independent source to reach PERSISTENT_TREND) — an honest consequence of reusing the classifier unmodified, not a defect. All 8 production-verification checks passed live against `data/fpl.db`, including a real captain reaching MATERIAL decision-effect with a real applied +0.18xP through the canonical DecisionSnapshot. 87 new tests, full suite 1411/1411 green.
- [40-session-2026-09-02-football-intelligence-engine-first-pass.md](40-session-2026-09-02-football-intelligence-engine-first-pass.md) — 2026-09-02: audited the existing qualitative data flow (found a sound persistence system already existed, revised the initial assumption it didn't) and fixed a real duplicate-signal bug (`statistical_evidence.py`'s dedup was scoped to its own analysis_version, blind to the LLM skill's own differently-tagged rows for the same evidence) and a stale decision_fusion explanation. New canonical `models/football_signal.py::FootballSignal` — a real unifying view (not new storage) with OBSERVATION/INTERPRETATION/FPL_EFFECT/DECISION_EFFECT kept explicit, plus `classify_decision_effect` bridging to the Phase 2 `DecisionSnapshot`'s own already-computed margins. 19 new tests.
- [39-session-2026-09-02-decision-engine-rebuild.md](39-session-2026-09-02-decision-engine-rebuild.md) — 2026-09-02: audited then fixed a real critical bug — `_squad_gw_ev` (the value function behind every multi-GW path/step) had no XI/captain/bench awareness at all, affecting every real multi-GW recommendation; fixed via a new `resolve_gw_xi` + a rewritten, genuinely XI+captain+bench-weighted `_squad_gw_ev`. New canonical `optimization/decision_snapshot.py::DecisionSnapshot` — one authoritative, reproducible decision object reading the same hysteresis-stable source the dashboard's own verdict uses, proven consistent by a dedicated test. New `evaluate_user_scenario` for "what if I do X instead" without a second optimizer.
- [38-session-2026-09-02-projection-engine-forensic-audit.md](38-session-2026-09-02-projection-engine-forensic-audit.md) — 2026-09-02: forensic audit of the player xP model found and fixed 3 real bugs — an event-conflation bug in `locked_squad.py` that projected against the wrong gameweek for a mid-GW-synced squad (confirmed live: dashboard showed 40.6 pts where the real number was 46.1+), a minutes-bucket zero-shrinkage bug (thin real samples swinging to extreme probabilities with no prior blend), and a DefCon historical-data contamination bug (pre-2024/25 seasons treated as real zeros when DefCon wasn't tracked at all then).
- [37-session-2026-09-02-autonomous-runtime.md](37-session-2026-09-02-autonomous-runtime.md) — 2026-09-02: made the system runtime-independent of a Claude Code session — real free-transfer off-by-one bug fixed (a phantom GW1 FT, now backed by real synced transfer history instead of inference), `monitoring/readiness.py`'s hardcoded fake statuses replaced with real live-exercised checks, two real Task Scheduler result-code misreadings fixed, and a decision-freshness watching gap closed (a recommendation's own target player, not just the current squad, now triggers staleness).
- [36-session-2026-08-29-recomputing-severity-overload-bug.md](36-session-2026-08-29-recomputing-severity-overload-bug.md) — 2026-08-29 (direct follow-up: "why does the dashboard again say recomputing"): real bug - `has_material_change_since` (freshness check + auto-recompute trigger, shared by both) treated a `kickoff_reminder` change_event as material because it's deliberately hardcoded HIGH severity for a DIFFERENT consumer (the alerts panel) - a kickoff happening carries zero new player/price/lineup information. Excluded that event type explicitly. Also traced a real leftover: the OLD buggy code had already fired a real background `fpl strategic-plan` subprocess before the fix landed, so the banner correctly stayed up (real 15-min self-heal lock, not a second bug) until that lock aged out and the process's own real ~15min beam search (under this session's own heavy concurrent load) settled. 3 new tests, 115 green.
- [35-session-2026-08-29-apexcharts-visualization-rebuild.md](35-session-2026-08-29-apexcharts-visualization-rebuild.md) — 2026-08-29 (direct escalation: "so is there no better amazing python libraries... hot dogshit", then a 26-point spec demanding a full visualization-system rebuild, not another polish pass; later the same day, a bug report - "no squad still pops up, game online but not on dashboard" - plus an even more detailed 14-chart follow-up spec; finally "commit and ensure everything runs automatically... even if i shut down my laptop"): real inventory of every dashboard visualization done first. Redesigned all 7 existing quantitative charts with the correct real chart type per data semantics and built 7 genuinely new ones across both passes (projection range, Dixon-Coles team-strength bar, xG-vs-xA scatter, player-form line, captain impact, player value, fixture-difficulty heatmap) from real, already-computed or already-stored data - zero fabrication. Found and fixed 8 real bugs live via actual browser console/screenshots (config-nesting, id-space mismatch, unsupported JS-function opacity, stale-process SSE, axis-precision collapse, a fixed-bucket-width chart unreadable over a multi-day GW, a `baseChart` stacked-flag miss, and self-caught mojibake in a new tooltip) plus 2 real functional bugs (`live-match-poll` never regenerating the dashboard for a match's OWN first LIVE transition - the direct cause of "game online but not on dashboard" - and the `locked_squad.py` retry needing a real sleep between attempts under this project's own genuine concurrent load). New persistent `window.dashboardCharts` registry with real incremental `appendData` live updates. Closed a genuine reboot-survival gap via a non-elevated Windows Startup-folder shortcut after confirming live that the cleaner fix (an `AtLogOn` scheduled-task trigger) needs Administrator rights this session doesn't have. Full responsive QA at 1440/1024/768/390px. 1320/1320 tests green.
- [34-session-2026-08-29-autonomy-correction-pass.md](34-session-2026-08-29-autonomy-correction-pass.md) — 2026-08-29 (emphatic autonomy directive, then a fresh "no squad / live tracking stuck / graphs look like poop" report right after fixes were claimed done): real root cause found — `fpl live-server` had never actually been registered in Task Scheduler (the setup script existed but was never run) and `fpl live-match-poll`'s own long-running instance predated every fix made this session; both persistent processes were running stale, pre-fix in-memory code the whole time regardless of what landed on disk. Fixed: registered `FPLAgentLiveServer` for real, killed both stale processes, confirmed fresh instances pick up every fix (crests, Chart.js, freshness/hysteresis, shot-map clamp, momentum padding, match-feed trim) via a real browser screenshot pass. Corrected a real logic bug in the previous session's draft `locked_squad.py` retry fix (it retried a pure function with identical input — a no-op). Found the "recurring zero-picks warning" diagnosis was partly test-log pollution (shared `logs/fpl_agent.log` between pytest and production). 1311+112 tests green.
- [33-session-2026-08-29-visual-quality-correction.md](33-session-2026-08-29-visual-quality-correction.md) — 2026-08-29 (direct harsh follow-up: "graphs look terrible... where are the team crests... team outlook dashes... still says recomputing"): confirmed RECOMPUTING was real-but-transient, already resolved, not stuck. Real crest fix (not the earlier text-monogram substitute) — the CDN only 403s a BROWSER's cross-origin fetch; a server-side fetch succeeds, cached via new `fpl sync-crests` + `ingestion/crest_assets.py`, wired into every crest call site dashboard-wide including Live Tracking rows. Two real bugs found+fixed en route (a test-isolation DATA_DIR bug, an invisible-monogram CSS-class bug). Team Outlook's Attack/Defence columns removed (FPL's data is genuinely unpublished, dashes read as broken). Rank/points/captain/actual-vs-expected charts rewritten from hand-rolled SVG to real Chart.js (vendored locally, MIT). 1306/1306 tests green.
- [00-foundation-phases-1-9.md](00-foundation-phases-1-9.md) — Phases 1-9: foundation build (data model, optimisation, Claude Code layer, live ops, reliability, first-team-ready)
- [01-pillar-0-1-prediction-decision-intelligence.md](01-pillar-0-1-prediction-decision-intelligence.md) — Pillar 0-1: prediction accuracy core (Dixon-Coles/odds/shrinkage) + decision intelligence (transfer search, scenario engine, chip DP, sampled EO)
- [02-pillar-2-3-and-preseason-calibration.md](02-pillar-2-3-and-preseason-calibration.md) — Pillar 2-3: Tier 2-4 news connectors, live odds, preseason calibration (squad churn, cross-league priors), live-ops maturity
- [03-limitations-and-pre-gw1-fixes.md](03-limitations-and-pre-gw1-fixes.md) — Competitor-scope audits, ML-ensemble experiment, backtest correction, and the full pre-GW1 known-limitations snapshot
- [04-session-2026-08-20-21.md](04-session-2026-08-20-21.md) — 2026-08-20/21: pre-GW1 hardening, chip-horizon correction, squad finalization, multi-GW squad build, predicted lineups, team outlook, dashboard v3
- [05-session-2026-08-21-live-gameweek-layer.md](05-session-2026-08-21-live-gameweek-layer.md) — 2026-08-21: forensic audit, floor/ceiling fix, perf work, real my-team integration, rotation-risk signal, official shirt graphics, final explicit squad
- [06-session-2026-08-21-22-pillar4-dashboard-state.md](06-session-2026-08-21-22-pillar4-dashboard-state.md) — 2026-08-21/22: live-gameweek layer, dashboard visual revamp + premium redesign, Pillar 4 match intelligence, dashboard-state architecture
- [07-session-2026-08-22-automation-and-dashboard.md](07-session-2026-08-22-automation-and-dashboard.md) — 2026-08-22: matchday autonomy (analysis queue, auto-discovery, live poller), player/team/manager intelligence, decision fusion, automation lifecycle
- [08-session-2026-08-26-gw1-postmortem-audits.md](08-session-2026-08-26-gw1-postmortem-audits.md) — 2026-08-26: GW1 postmortem (cold-start fix, transfer fusion), GW2-accuracy audit, P0/P1 gap-audit implementation, decision-quality audits
- [09-session-2026-08-27-strategic-planner-and-dashboard.md](09-session-2026-08-27-strategic-planner-and-dashboard.md) — 2026-08-27: value-of-information + chip-horizon bug, multi-GW strategic path search, dashboard redesign into Primary Decision/Strategy Explorer/Squad State Machine/Intelligence/Market
- [10-session-2026-08-27-joint-chip-optimization-and-decision-hierarchy.md](10-session-2026-08-27-joint-chip-optimization-and-decision-hierarchy.md) — 2026-08-27: joint chip+transfer optimization inside the beam search, full-squad starting-action comparison, single authoritative CURRENT RECOMMENDED ACTION, dashboard match-intelligence text-density fix
- [11-session-2026-08-27-adversarial-decision-audit.md](11-session-2026-08-27-adversarial-decision-audit.md) — 2026-08-27: Adversarial Decision Audit (`fpl decision-audit`) — causal trace, per-player MODEL-vs-FOOTBALL, analytic stress-test falsifiers, multi-horizon action audit, league-wide/cold-start/qualitative checks, scorecard; the audit's own first production run caught a real methodology bug in itself (chip full-ILP-rebuild vs beam-limited transfer search), fixed and permanently cross-checked
- [12-session-2026-08-27-adversarial-audit-search-space-fix.md](12-session-2026-08-27-adversarial-audit-search-space-fix.md) — 2026-08-27 (follow-up): closed the search-space gap properly — `alternative_action_audit` now runs the same real `known_paths` wide-search cross-reference `fpl strategic-plan` uses; verified live against the real production DB that the audit's ranking now matches a fresh strategic-plan run exactly, while the earlier flagged disagreement is confirmed to have been genuine cache staleness, not a fix artifact
- [13-session-2026-08-27-frontend-redesign-phase1.md](13-session-2026-08-27-frontend-redesign-phase1.md) — 2026-08-27: Frontend redesign Phase 1 — dashboard.py split into a `monitoring/dashboard/` package, new HOME/PLAN/SQUAD workspaces (task-oriented nav, structured-fact copy, one embedded decision-snapshot JSON payload, real path/GW click interactivity) replacing the old Primary Decision/Strategy Explorer/Squad State Machine panels; real `html{font-size:18px}` type-hierarchy bug found and fixed during live visual QA; 1008/1008 tests
- [14-session-2026-08-28-frontend-redesign-phase2.md](14-session-2026-08-28-frontend-redesign-phase2.md) — 2026-08-28: Frontend redesign Phase 2 — Intelligence workspace rebuilt into a real league-wide team-signal briefing (reuses `team_outlook` across all teams, not just squad), Opportunity board redesigned with real per-card confidence and a 1-visible-per-category cap, Market reframed, Fixture Tool gets real range/metric(overall-attack-defence, honestly disclosing FPL's current preseason data gap)/sort/filter controls; real badge-URL bug (team_id vs team_code) caught by the module's own test before shipping; 4 legacy orchestrator functions retired; 1014/1014 tests
- [15-session-2026-08-28-visual-density-and-accuracy-audit.md](15-session-2026-08-28-visual-density-and-accuracy-audit.md) — 2026-08-28: Squad's projected-GW previews rebuilt into real shirt tiles; real-source audit of elevenify.com/Spreadex (fpl.page's own credited sources) confirmed neither is viable as a free automated backend feed; concrete GW2 Haaland-vs-Mbeumo captaincy spot-check traced to a real, mechanically-correct fixture-driven cause, not a bug; ~100 sub-12px CSS font-sizes bumped to a real fpl.page-measured 12px floor, crests bumped from 18px to 32-40px (fpl.page measured at 24-40px), player shirts/crests added to the Opportunity Board; 1024/1024 tests
- [16-session-2026-08-28-dc-shrinkage-and-no-squad-bug.md](16-session-2026-08-28-dc-shrinkage-and-no-squad-bug.md) — 2026-08-28: fixed a real, confirmed Dixon-Coles small-sample overfit bug (Hull's real GW1 2-0 shock win over Man Utd drove its clean-sheet projection to an implausible 84% three gameweeks later off n=1 match) with a real L2 ridge prior, carefully scaled to preserve an existing performance-optimization's exact mathematical invariant; verified zero regression via a real backtest A/B run; hardened a real crash path in `get_locked_squad`/`_xi_from_real_picks` (NULL squad_slot) with a regression test and added diagnostic logging for the "no squad" report; confirmed via direct research that Understat (already used) remains the best free xG source available - the real bug was a calibration gap, not a wrong data source; 1019/1019 tests
- [17-session-2026-08-28-new-panels.md](17-session-2026-08-28-new-panels.md) — 2026-08-28: four new league-wide dashboard panels built from real, already-synced data per a direct fpl.page screenshot comparison - Injuries, Expected Data (xG/xA/xGI), Team Odds, Top Transfers In/Out; Price Changes' rich filter UI and a real Odds Tracker history chart deliberately deferred and disclosed (real UI/infra work, not a data re-skin); 1024/1024 tests
- [18-session-2026-08-29-chip-mapping-freshness-and-projected-xi.md](18-session-2026-08-29-chip-mapping-freshness-and-projected-xi.md) — 2026-08-29: real chip-mapping bug fixed (Plan/Squad rendered the independent `schedule_chips` DP cross-check instead of each path's own `chip_played`), real decision-freshness disclosure + staleness banner added (`models/decision_freshness.py`), real per-GW starting XI/bench/captain/vice resolution replacing "carries over unchanged" (`optimization/squad.py::resolve_projected_xi`); captain-model and new-transfer/regime audits re-confirmed prior findings hold; path-diversity duplication live-reconfirmed as still open; 1043/1043 tests
- [19-session-2026-08-29-path-diversity-and-p1-audit.md](19-session-2026-08-29-path-diversity-and-p1-audit.md) — 2026-08-29 (same-day continuation): path-diversity bug fixed (`build_diverse_paths` ranks `compare_starting_actions`' real per-action options instead of the raw beam's near-duplicate top-N), real per-path 3/5/8GW breakdown added (`checkpoint_breakdown`, bounded to selected paths only); P1 audit found the copy layer/visual-component-diversity/typography asks already substantially satisfied by prior sessions, fixed 2 real small gaps (Opportunity Board "considered by optimizer" flag, Value category excluding squad members), confirmed no data-quality or performance regressions; live-verified against production decision #102; 1058+ tests
- [20-session-2026-08-29-locked-squad-torn-read-fix.md](20-session-2026-08-29-locked-squad-torn-read-fix.md) — 2026-08-29 (same-day, direct user bug report): root-caused and fixed the real intermittent "dashboard randomly shows no squad, refreshing doesn't help" bug - a genuine torn read in `ingestion/my_team.py::get_latest_squad` (two separate un-transacted SELECTs racing against the project's own real, independently-scheduled `run-scheduled` writer), fixed by making it one atomic statement; added a diagnostic warning for the narrower residual race and raised the sqlite connection timeout as defensive hardening; explains "refreshing doesn't work" precisely (dashboard.html is a static file, only regenerated periodically)
- [21-session-2026-08-29-automation-audit-and-optimizer-auto-recompute.md](21-session-2026-08-29-automation-audit-and-optimizer-auto-recompute.md) — 2026-08-29 (same-day, "master automation pass"): the optimizer now runs automatically (`_maybe_trigger_strategic_plan_recompute`, materiality-gated, detached background subprocess, wired into `run_scheduled`) - real gap closed, live-verified against production; live-rank cadence tightened to a genuine ~5min during an active GW (via the already-running fast `live-match-poll` loop); `scheduler-status` now reports both real daemon tasks; real end-to-end CURRENT_FPL_STATE invariant tests added. Real architecture audit (per the spec's own "inspect first" instruction) found most of the requested math/optimizer capabilities already built: a real probabilistic minutes model, correlated Monte Carlo scenario sampling, an adaptive deadline/live-window-aware scheduler that re-registers itself, and working calibration-capture infrastructure (15 real samples so far - too few for curves, a data-volume limit not an engineering one)
- [22-session-2026-08-29-fpl-page-parity-p1-features.md](22-session-2026-08-29-fpl-page-parity-p1-features.md) — 2026-08-29 (continuation of session 19's P1 audit list): 6 genuinely-unbuilt fpl.page-parity features closed - Points Changes (new, real post-match Bonus/DefCon revision ledger off already-ingested `player_stats_snapshot`, 0 bonus + 8 real DEFCON revisions live-verified in GW1), Template Team + elite-manager context (per-position EO pool with real margin-of-error surfaced, honest raw-ownership fallback since `fpl sync-eo` hasn't run yet this season), Price History (league-wide search/filter/progress-bar forecast table + real confirmed-change ledger, both off already-real data, replacing the old squad-only panel), news decision-impact tags (captain/transfer-out/transfer-in, reusing the already-computed decision objects), Fixture Tool Goals/CS% view toggle, and closed the last two P1 checklist items (responsive QA at 1440/1024/768/360px, 26 confirmed-dead CSS rules + 2 orphaned functions removed via a scripted audit). Found+fixed a real `UnboundLocalError` this session's own news-enrichment change introduced, and one now-too-broad pre-existing test assertion. 1197/1197 tests green, live-verified via a real localhost-served dashboard render against production data
- [23-session-2026-08-29-final-decision-system-completion-pass.md](23-session-2026-08-29-final-decision-system-completion-pass.md) — 2026-08-29 (direct "final product + decision system completion" spec, fpl.page as primary reference): audited first - several spec premises (fake countdowns, no live-change-feed, no strategy-family clustering, no burst-coalescing) turned out already solved by prior sessions, verified not rebuilt. Built: SYSTEM LIVE strip News/Projections fields; Points Changes real LIVE/EXPIRED status (fpl.page's own published 1h-lock rule, their real "Pending" state explicitly NOT built - needs Opta's raw feed, no access) wired into `live_snapshot.json` + the live-changes feed; chip strategy why-now/best-alternative/opportunity-cost explainability (`ChipExplanation` was already computed and logged, never read by any panel before this); a new real MODEL vs FOOTBALL/MARKET(Solio)/TEMPLATE cross-check on the Home hero (pure synthesis over three already-computed results, never averaged); Template Team overlap/differential; Gameweek Projections 3/5/8GW range control. Disclosed, not attempted: full BUY/HOLD/SELL/WATCH/REVIEW Player Inspector vocabulary, intragame live-chart time-series storage, Fixture Ticker rotation analysis (no real rotation-risk model exists), full Intelligence/News-pipeline editorial restructuring, a dedicated visual-redesign audit
- [24-session-2026-08-29-continuation-perf-fix-and-more-parity.md](24-session-2026-08-29-continuation-perf-fix-and-more-parity.md) — 2026-08-29 (same-day continuation, "continue with everything remaining"): **major real perf bug found + fixed** - `get_locked_squad()` was silently running a full ~600-player Dixon-Coles/Monte-Carlo xP scan (~6.2s) on every ~20-25s live-match tick via `live_snapshot.py`, violating that module's own "stay cheap" contract; fixed to the already-existing `build_player_pool_for_ids` scoped to the 15 known picks (~26x faster, 6.6s→223ms end to end). New: Fixture Tool Rotation sort + Reset (reuses the real blank/double-GW detector, no fabricated rotation-risk model), Opportunity Board "MY SQUAD IMPACT" (reuses `ta.candidates`), a real intragame live-rank chart (the decisions journal already had the time series, just never charted), Player Inspector FOOTBALL section. Dead-CSS audit round 2 (~35 more confirmed-dead rules, mostly a pre-Plan-workspace "Strategy Explorer" remnant) found a second real bug: the Opportunity Board's Value cards were silently missing their kind-label color (`.opp-card-price` never matched - real class is `.opp-card-value`). Verified (not rebuilt): Intelligence panel's WHAT CHANGED/WHY/IMPACT/CONFIDENCE structure and source-health primary/secondary split were both already built. 1219/1219 tests green
- [25-session-2026-08-29-remaining-limitations-closed.md](25-session-2026-08-29-remaining-limitations-closed.md) — 2026-08-29 (same-day continuation, "continue with all remaining limitations"): closed 4 of 5 disclosed gaps. Captain contribution + starting-XI actual-vs-expected charts (real per-finished-GW grain, no new storage needed - `prediction_outcomes`/`my_team_picks` already had it; only the intragame/sub-tick version still needs new storage). Player Inspector gained a real MARKET row (Solio comparison) and a real REVIEW state (reuses `ta.evidence_confidence`) - BUY deliberately not added anywhere, would violate the project's own "no panel shows a second competing recommendation" rule. News panel gained the missing STATE CHANGE + MODEL IMPACT link (`change_events` correlated within 48h of a matched article, reuses the row's own real `fpl_impact` text). Visual-redesign audit: compared real fpl.page screenshots directly against our own render, found one genuine structural gap (Price History was a flex-row list, not a real table) and fixed it, reusing the existing `.xdata-table` component - found + fixed a real CSS bug in the process (`.price-progress-track`'s `flex-basis` did nothing outside a flex container). Fifth item (full systematic CSS sweep) intentionally left narrower than "everything" - disclosed, not fabricated as done
- [31-session-2026-08-29-live-architecture-milestones-4-6.md](31-session-2026-08-29-live-architecture-milestones-4-6.md) — 2026-08-29 (same-day continuation, "continue everything left" - all 3 remaining candidates closed in one pass): **(4) decision hysteresis** (spec section 11) - new `models/decision_hysteresis.py::stable_current_recommendation`, requires a real min EV advantage (5.0pts)/confidence floor (MEDIUM+)/persistence (>=2 consecutive decisions) before replacing an already-stable recommendation, wired ONLY into the dashboard's own primary verdict (not the audit/freshness tools, which deliberately want raw latest reality). **(5) live feed SSE rendering** - the milestone-2 `change_event`/`match_event` channels now render in the browser, reusing the exact same dedup key the poll-based feed already uses for `change_event` (genuine dedup against the same real row arriving twice) and a real new key for `match_event` (FotMob's own richer per-incident text, genuinely additive). **(6) provider abstraction** - new `providers/football.py` (`FootballDataProvider` Protocol + `FotMobProvider`, dynamic module lookup so existing monkeypatch-based tests keep working) and `providers/fpl.py` (real finding: `ingestion/fpl_api.py::FPLApiAdapter` from an earlier session already satisfies this shape, aliased not reimplemented); `sync_match` gained an optional `provider=` param, genuine swappability proven by injecting and confirming a real fake provider gets called instead of FotMob. 13 new tests, full suite green, zero regressions in the wider dashboard/SSE suites
- [30-session-2026-08-29-live-architecture-milestone-3.md](30-session-2026-08-29-live-architecture-milestone-3.md) — 2026-08-29 (same-day continuation, "continue" after milestone 2 - user picked the materiality-gate wiring): new `live/materiality_engine.py` - makes the already-real, already-tested `_maybe_trigger_strategic_plan_recompute` gate reactive by subscribing it to `AVAILABILITY_CHANGED`/`PLAYER_STATE_CHANGED`/`LINEUP_CONFIRMED`/`PRICE_CHANGED`/`FIXTURE_CHANGED` on the milestone-1 event bus, instead of only checking once per `run_scheduled` tick. Deliberately does NOT alter the trigger logic itself, and deliberately does NOT route GOAL/ASSIST/CARD/SUBSTITUTION/SHOT (spec's own routing table - those are FAST-ENGINE concerns, never worth a real beam search on their own). Registered at the three real event-producing entry points. 4 new tests, careful never to let the real (subprocess-spawning) trigger function actually run inside a unit test - proven via a monkeypatched stub instead, with a coverage check that every real `change_detection.py`-emittable event type is provably routed
- [29-session-2026-08-29-live-architecture-milestone-2.md](29-session-2026-08-29-live-architecture-milestone-2.md) — 2026-08-29 (same-day continuation, "continue" after milestone 1's report - user picked SSE transport as the next milestone): new `fpl live-server` CLI command + `live/sse_server.py` - a real, stdlib-only `ThreadingHTTPServer` exposing a real `/events` Server-Sent-Events stream and serving the dashboard's static files, deliberately a SEPARATE OS process from `live-match-poll` per the spec's own "keep the persistent worker and the dashboard/API separate" allowance. Since milestone 1's in-process event bus can't span processes, `DbTailer` bridges the gap by tailing the same real, already-persisted `match_events`/`change_events` tables + `live_snapshot.json`'s mtime - simultaneously the transport AND the real reconciliation mechanism the spec's own section 9 asks for. Real bug found + fixed via this milestone's own tests: a genuine startup race where a row inserted between server-start and tailer-thread-init was wrongly treated as "already known" - fixed by capturing baselines synchronously in `LiveServer.__init__` before `.start()` returns. Also found (test-only): `requests`/urllib3 never received data from this server's real HTTP/1.0 no-Content-Length SSE response even though a raw socket (and a real browser's native `EventSource`) does - streaming tests use `socket` instead. Browser wiring: a new minimal `EventSource` reuses the existing `applySnapshot` function directly for near-immediate snapshot updates; the existing ~10s poll is completely unchanged, now genuinely the fallback path. Scheduler scripts prepared, not registered (a persistent system change is the user's call). 1274 tests green (7 new), plus a real smoke test against the actual production `data/` directory
- [28-session-2026-08-29-live-architecture-milestone-1.md](28-session-2026-08-29-live-architecture-milestone-1.md) — 2026-08-29 (same-day, "live architecture rebuild" spec, milestone 1 of an explicitly incremental multi-session rebuild - zero UI changes): new `events/` package (real typed `EventType` vocabulary + a synchronous in-process `EventBus` - the correct scope for this single-process CLI app, not a distributed broker) and `live/fast_engine.py` + migration 0036 (`live_player_state` - a real SUBSTITUTION/MATCH_FINISHED-driven "this player's match minutes are now locked" fact this project previously computed via `player_match_state.substituted_off_minute` and never read anywhere). `fotmob_source.py::sync_match` now diffs against `match_events`'s own real UNIQUE key to detect genuinely new incidents and publishes typed GOAL/ASSIST/CARD/SUBSTITUTION/MATCH_STARTED/HALFTIME/RESUMED/FINISHED events (assist via FotMob's own real `assistPlayerId`, confirmed live); `change_detection.py` now also publishes PRICE_CHANGED/AVAILABILITY_CHANGED/LINEUP_CONFIRMED/FIXTURE_CHANGED onto the SAME bus. Real bug found + fixed via this milestone's own deterministic E2E test (`test_events_pipeline.py`, a real 3-tick FotMob payload sequence through the actual `sync_match` path): a fixture's very first sync (often already LIVE) never fired MATCH_STARTED because the transition check required a non-null prior status. 1267 tests green (7 new). Not done, real and disclosed: SSE/WebSocket transport, FPL-side provider abstraction, materiality-engine consolidation onto the new bus, decision hysteresis, reconciliation loop, real live-match verification (none was live this session)
- [27-session-2026-08-29-fotmob-live-match-centre.md](27-session-2026-08-29-fotmob-live-match-centre.md) — 2026-08-29 (same-day continuation, "FotMob data extraction + live Match Centre" overhaul): real live-tested FotMob investigation found `content.momentum`/`content.shotmap`/`content.playerStats` already present in the SAME payload `sync_match` fetches every sync but never parsed (migration 0035, real per-minute momentum + per-shot x/y/xG storage) - real rating/minutes/assists/xA/chances-created were hardcoded to `None` in `parse_player_states` despite the schema already supporting them, plus a real upsert bug found+fixed (`rating` missing from `ON CONFLICT DO UPDATE SET`, never refreshed on re-sync). New `monitoring/dashboard/match_centre.py` (real live score/stats/momentum-chart/shot-map/my-players panel, single-sourced from a new `live_snapshot._active_matches_block`) - found+fixed a real wiring bug (gated behind the wrong state variable) and a real shot-map dot-size bug during live verification (temporarily flipping a real finished match to LIVE, screenshotting, reverting). Real torn-read fix: `_write_dashboard` wraps `generate_dashboard_html` in an explicit transaction (confirmed that call tree is genuinely read-only) so a concurrent scheduled writer can't make one render combine new-and-old field values. `live-match-poll` interval tightened 25s->15s. 1257/1257 tests green
- [32-session-2026-08-29-forensic-visual-redesign.md](32-session-2026-08-29-forensic-visual-redesign.md) — 2026-08-29 (forensic visual/product redesign, fpl.page reference): System Live strip collapsed into a compact primary line + collapsed details disclosure; hero-metric/cross-check-pill polish; new `--fs-*` type-scale tokens (applied to Match Centre this pass); Match Centre re-audited live via the flip-to-LIVE technique — card border/divider, team-colour-filled badges, pill-shaped muted stat bars; real bug hunt during the re-audit found two false alarms (stale pre-fix Substitution event rows needing a re-sync, not a regression; a mangled-looking name that was actually correct UTF-8, just a terminal codepage artifact) and one real fix (nav-bar right-edge scroll-fade affordance below ~900px). Responsive-swept 1440/1024/768/390 — 768's nav overflow was the one real breakpoint finding, fixed.
- [26-session-2026-08-29-live-command-centre-pass.md](26-session-2026-08-29-live-command-centre-pass.md) — 2026-08-29 (same-day, "live GW command centre" runtime/UI overhaul): real bug found + fixed - the browser's "Next check" live-poll countdown could reach 0s and freeze there forever (`nextPollAt` only advanced on a successful poll; a failed/absent fetch, common outside a live match, froze it permanently); now resets on every poll attempt, shows "delayed - retrying" on a genuine regression. Readable "Data health · N issue(s)" replaces the old raw `"N source(s) degraded: fpl_api_my_team, livefpl, ..."` connector-name dump - self-caught a regression where the first version rebuilt that element every 1s tick, destroying its own `<details>` open state. Real chart-legibility fix (`live_charts.py`/CSS) - charts could render as small as ~70-90px tall at a narrow grid column; floored at 240px with a wider minimum column, live-verified at desktop and mobile. Real flat-vs-gradient contradiction fixed on the Home hero (purple gradient despite the header's own "flat, no gradient" rebuild). Real FotMob xG/xA/shots/key-passes (already fetched, never displayed) surfaced on squad players' live rows; new compact real MATCH STATS panel (possession/shots/xG/corners, `team_match_state`) - momentum/shot-map/heatmap investigated and found genuinely not parsed yet, disclosed rather than fabricated. 1245/1245 tests green. No GW was actually live this session - verified the poll/countdown/patch mechanics directly rather than against a genuinely advancing match; also observed one transient torn-read-class "NO SQUAD" render, self-healed on immediate re-run, confirmed not caused by this session's edits


